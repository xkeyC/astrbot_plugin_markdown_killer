{
    "enable_global_markdown_killer": {
        "description": "全局 Markdown 移除",
        "type": "bool",
        "default": false,
        "hint": "开启后，将从所有即将发送的最终文本消息中移除 Markdown 格式（而不仅限于 LLM 直接输出的内容）。"
    },
    "remove_extra_newlines": {
        "description": "移除多余的换行符",
        "type": "bool",
        "default": true,
        "hint": "启用后，将移除文本中多余的换行，避免分段发送时出现过多空行"
    },
    "newline_mode": {
        "description": "换行处理模式",
        "type": "string",
        "default": "segment_boundary",
        "hint": "segment_boundary: 只移除分段标点后的换行（推荐）；global: 全局压缩连续空行",
        "options": [
            "segment_boundary",
            "global"
        ]
    },
    "enable_table_render": {
        "description": "启用表格图片渲染",
        "type": "bool",
        "default": true,
        "hint": "开启后，LLM 回复中的 Markdown 表格将使用 Playwright 浏览器渲染为图片并嵌入文本消息中。需要执行 `python -m playwright install chromium` 安装浏览器（首次启用时插件会尝试自动安装）。"
    },
    "table_render_fallback": {
        "description": "表格渲染失败回退策略",
        "type": "string",
        "default": "text",
        "hint": "当表格图片渲染失败（如浏览器未就绪/超时）时的处理方式：text=转为纯文本（去分隔行，保留单元格内容）；raw=保留原始 Markdown 表格文本；remove=直接丢弃表格内容",
        "options": [
            "text",
            "raw",
            "remove"
        ]
    }
}
